//9. For – Quadrado e Cubo//

for (let numero = 2; numero <= 30; numero += 2) {
    let quadrado = numero ** 2;
    let cubo = numero ** 3;

    console.log(numero + "² = " + quadrado + " | " + numero + "³ = " + cubo);
}
